@model TAT001.Entities.TAX_LAND

@{
    ViewBag.pagina_r = 502;
    ViewBag.carpeta_r = 500;
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="col 12 s12 ">
        <div class="card-panel">
            <div class="row">
                <div class="form-horizontal">
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-group col l6">
                        @Html.LabelFor(model => model.PAIS_ID, "Sociedad", htmlAttributes: new { @class = "control-label col-md-2" })
                        @Html.DropDownList("Sociedad", null, htmlAttributes: new { @class = "form-control" })
                        @Html.ValidationMessageFor(model => model.PAIS_ID, "", new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Create" class="btn btn-default" />
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
<script>
    var elem = document.querySelectorAll('select');
    var instance = M.Select.init(elem, []);
</script>
<div>
    @Html.ActionLink("Back to List", "Index")
</div>
