@model TAT001.Entities.TAX_LAND
@{
    ViewBag.pagina_r = 812;
    ViewBag.carpeta_r = 500;
    //ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="row">
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <button id="btnSave" type="submit" value="Create" class="btn btn-default" style="float:right;"></button>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col s8 ">
            <div class="card-panel">
                <div class="row">
                    <div class="form-horizontal">
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        <div class="form-group col l10">
                            @Html.LabelFor(model => model.PAIS_ID, "Sociedad", htmlAttributes: new { @class = "control-label col-md-2", @id = "lblSociedadId" })
                            @Html.DropDownList("Sociedad", null, htmlAttributes: new { @class = "form-control" })
                            @Html.ValidationMessageFor(model => model.PAIS_ID, "", new { @class = "text-danger" })
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
<script>
    var elem = document.querySelectorAll('select');
    var instance = M.Select.init(elem, []);
</script>
<div>
    @Html.ActionLink("Back to List", "Index", null, new { id = "lblRegresar" })
</div>
